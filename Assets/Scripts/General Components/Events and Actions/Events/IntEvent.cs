using UnityEngine;

namespace GeneralComponents
{
    [CreateAssetMenu(fileName = "New Int Game Event", menuName = "General Components / Events / Int Event", order = 1)]
    public class IntEvent : GameEvent<int> { } 
}
